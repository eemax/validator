# Public template for private fetch query params.
# Copy to .local/fetch-params.yml or keep it in a private repo and point
# CENTRIC_FETCH_PARAMS at it.

endpoints:
  styles:
    query_params:
      active: true
      custom_style_status: production
    count_query_params:
      active: true
      custom_style_status: production

  materials:
    query_params:
      active: true
      custom_material_status: approved
    count_query_params:
      active: true
      custom_material_status: approved

  colorways:
    query_params:
      active: true
    count_query_params:
      active: true
